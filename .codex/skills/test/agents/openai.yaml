interface:
  display_name: "Test"
  short_description: "Run the repo QC suite and e2e verification"
  default_prompt: "Run the repository quality checks through scripts/dev.py in the default concise mode, use --verbose only when concise failure diagnostics are insufficient, summarize failures clearly, assess coverage gaps, and report whether e2e passed."
